require 'spec_helper'
require 'github_heroku_deployer/git'

describe GithubHerokuDeployer::Git do
  # it { should respond_to :push_app_to_heroku }
  # it { should respond_to :open_or_setup }
  # it { should respond_to :wrapped_clone }
  # it { should respond_to :wrapped_push }
  # it { should respond_to :local_state }
  
  describe "push_app_to_heroku" do
  end

  describe "open_or_setup" do
  end

  describe "wrapped_clone" do
  end

  describe "wrapped_push" do
  end

  describe "local_state" do
  end
end
